{
  "name": "cloud-mail-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run build",
    "build": "npx --yes pnpm@9.15.9 --prefix ./mail-worker install --frozen-lockfile && npx --yes pnpm@9.15.9 --prefix ./mail-vue install --frozen-lockfile && npx --yes pnpm@9.15.9 --prefix ./mail-vue run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}