{
  "name": "cloud-mail-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run build",
    "verify:target": "node ./scripts/verify-cloudflare-target.mjs",
    "build": "npm run verify:target && npx --yes pnpm@9.15.9 --prefix ./mail-worker install --frozen-lockfile && npx --yes pnpm@9.15.9 --prefix ./mail-vue install --frozen-lockfile && npx --yes pnpm@9.15.9 --prefix ./mail-vue run build",
    "deploy": "npm run verify:target && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}